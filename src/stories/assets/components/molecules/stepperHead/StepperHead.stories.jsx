import React from 'react';

import { StepperHead } from './StepperHead';

export default {
  title: 'molecules/StepperHead',
  component: StepperHead,
};

const Template = (args) => <StepperHead {...args} />;

export const Default = Template.bind({});