import React from 'react';

import { Paleta } from './Settings';

export default {
  title: 'Paleta',
  component: Paleta,
};

const Template = () => <Paleta />;

export const Colores = Template.bind();